- model_class = ProjectCode
.page-header
  %h1 Modifier Code Projets
=# render :partial => "form"
= simple_form_for @project_code, html: { class: 'form-horizontal' } do |f|
  = f.input :name
  .form-actions
    = f.submit "Mettre à jour", class: 'btn btn-primary'
    = link_to "Annuler", @project_code, class: 'btn'

