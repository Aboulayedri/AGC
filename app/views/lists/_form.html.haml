= simple_form_for @list, :html => { :class => 'form-horizontal' } do |f|
  = f.input :entity_id
  = f.input :date
  .form-actions
    = f.button :submit, :class => 'btn-primary'
    = link_to t('.cancel', :default => t("helpers.links.cancel")), lists_path, :class => 'btn'
